apiVersion: apps/v1
kind: Deployment
metadata:
  name: celery-worker-sparql
  namespace: toxindex-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: celery-worker-sparql
  template:
    metadata:
      labels:
        app: celery-worker-sparql
    spec:
      serviceAccountName: toxindex-app-sa
      containers:
      - name: celery-worker-sparql
        image: us-docker.pkg.dev/toxindex/toxindex-backend/sparql:latest
        command: [ "python", "-m", "celery", "-A", "workflows.celery_worker_sparql", "worker", "--loglevel=info", "-Q", "SPARQL"]
        envFrom:
        - secretRef:
            name: backend-secrets
        env:
        - name: REDIS_HOST
          valueFrom:
            secretKeyRef:
              name: backend-secrets
              key: REDIS_HOST
        - name: REDIS_PORT
          valueFrom:
            secretKeyRef:
              name: backend-secrets
              key: REDIS_PORT
        resources:
          requests:
            memory: "2Gi"
            cpu: "2000m"
          limits:
            memory: "8Gi"
            cpu: "2000m"
